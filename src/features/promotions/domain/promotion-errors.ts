/**
 * Spanish message shown when a chosen bonus promotion can no longer be sold —
 * it was archived, its window closed, or its tariff was archived — between the
 * form render and the server action re-resolving it against the BA business
 * date (spec: "promotions / Date window at sale time").
 */
export const PROMOTION_UNAVAILABLE_MESSAGE =
  "La promoción seleccionada ya no está disponible.";
